.jumbotron.center
  %span#title1
    %h1.glyphicon.glyphicon-fire Chestnut
  %p.lead
    #title2
      Your personal lending library.
  - if !current_user
    = link_to "<span class= 'glyphicon glyphicon-fire'></span> Register or sign in with Facebook!".html_safe, "/auth/facebook", id: "sign_in", :class => "btn btn-lg btn-success"
  - else
    = link_to "My Library", user_library_items_path(current_user), class: "btn btn-lg btn-primary"
- if !current_user
  .row.marketing
    = render 'marketing'
- else
  .row.marketing
    = render 'stream'
